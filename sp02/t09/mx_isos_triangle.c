#include <stdio.h>
#include <unistd.h>
#include <stdbool.h>
void mx_isos_triangle(unsigned int lenght, char c){
    for(unsigned int i = 1; i <= length; i++){
        for(unsigned int j = length - i; j < length; j++){
            mx_printchar(c);
        }
    mx_printchar('\n');
    }
}
